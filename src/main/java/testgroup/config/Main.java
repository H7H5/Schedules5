package testgroup.config;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;
import java.sql.Statement;

public class Main {
    //public static void main(String[] args) {
    //    //String url = "jdbc:mysql://localhost:3306/test";
    //    String url = "jdbc:postgresql://localhost:5432/mydbtest?serverTimezone=Europe/Minsk&useSSL=false";
    //    String username = "postgres";
    //    String password = "root";
    //    System.out.println("Connecting...");
//
    //    try (Connection connection = DriverManager.getConnection(url, username, password)) {
    //        System.out.println("Connection successful!");
    //        Statement statement = connection.createStatement();
    //        //statement.execute("insert into schedules (groupp, day, number, name, teacher, study,numerator)values('5-076','Tuesday',1,'practice','Serdyuk W.S.', '1-220','numerator');");
    //        statement.execute("");
    //    } catch (SQLException e) {
    //        System.out.println("Connection failed!");
    //        e.printStackTrace();
    //    }
   // }
}
